{
  "name": "Hawkeye",
  "version": "0.2.1",
  "scripts": {
    "start": "npm run build && ./watch.sh",
    "build": "rm -rf dest && tsc -p tsconfig.json && grunt build"
  },
  "devDependencies": {
    "grunt": "^1.0.1",
    "grunt-bower-task": "^0.4.0",
    "grunt-contrib-clean": "^1.0.0",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-contrib-watch": "^1.0.0",
    "typescript": "^1.8.10"
  }
}
